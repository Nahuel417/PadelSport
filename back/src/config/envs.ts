import 'dotenv/config';

export const PORT = process.env.PORT;

export const USERNAME = process.env.USERNAME;

export const POSTGRES_PASS = process.env.POSTGREST_PASS;

export const DATA_BASE = process.env.DATA_BASE;
